<UserControl x:Class="McTimelineDemo.Controls.SelectedItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- Last clicked item details -->
  <Border Padding="8"
          CornerRadius="6"
          Background="{ThemeResource ControlFillColorSecondaryBrush}">

    <Grid RowSpacing="4"
          ColumnSpacing="10">

      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <TextBlock Text="Last selected item"
                 Grid.Row="0"
                 Grid.Column="0"
                 FontWeight="Bold" />

      <TextBlock Grid.Row="0"
                 Grid.Column="1"
                 Text="{x:Bind ButtonName, Mode=OneWay}"
                 Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
      
      <TextBlock Grid.Row="1"
                 Grid.Column="0"
                 x:Name="PART_Title" />

      <TextBlock Grid.Row="1"
                 Grid.Column="1"
                 x:Name="PART_Description" />

      <TextBlock Grid.Row="2"
                 Grid.Column="0"
                 x:Name="PART_Range" />

      <TextBlock Grid.Row="2"
                 Grid.Column="1"
                 x:Name="PART_IdKey"
                 Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />

    </Grid>

  </Border>

</UserControl>
